import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    fontSize: 18,
    width: '100%',
    borderStyle: 'solid',
    borderBottomWidth: 1,
    borderBottomColor: '#aecdff',
    color: '#fff',
  }
});